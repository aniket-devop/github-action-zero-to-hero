name: Deploy Flask App

on:
workflow_dispatch:

jobs:
deploy:
runs-on: self-hosted

```
env:
  DOCKERHUB_USER: ${{ vars.DOCKERHUB_USERS }}

steps:
  - name: Checkout code
    uses: actions/checkout@v4

  - name: Debug files
    run: ls -la

  - name: Deploy with Docker Compose
    run: docker compose up -d --build
