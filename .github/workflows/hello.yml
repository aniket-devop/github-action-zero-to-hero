name: hello aniket

on:
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: say hello to user
        run: echo "hello to user"

      - name: say hello to username
        run: echo "hello $USER"
details :
runs-on : ubuntu-latest
steps :
    -name : show what is the version pf python ,docker, etc
    run : docker --version && python --version
