name: Docker Build & Push

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # 📥 Code checkout
      - name: Checkout Code
        uses: actions/checkout@v4

      # 🔐 Docker Hub login
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: your-dockerhub-username   # 👈 apna username daal
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 🐳 Build & Push image
      - name: Build & Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            your-dockerhub-username/github-actions-app:latest
            your-dockerhub-username/github-actions-app:${{ github.sha }}